# Git Workflow Guide для DelTran MVP

## Быстрый старт

Этот гайд объясняет, как работать с Git в проекте DelTran простыми словами.

---

## 🎯 Основные команды Git

### 1. Проверить статус (что изменилось?)

```bash
git status
```

**Что показывает:**
- Какие файлы изменены
- Какие файлы готовы к коммиту
- На какой ветке вы находитесь

**Пример вывода:**
```
On branch chore/cleanup-all-green
Changes not staged for commit:
  modified:   gateway-go/cmd/gateway/main.go
```

---

### 2. Добавить файлы для коммита (Stage)

```bash
# Добавить один файл
git add путь/к/файлу.txt

# Добавить все изменённые файлы
git add -A

# Добавить все файлы в текущей директории
git add .
```

**Аналогия:** Положить товары в корзину перед оплатой в магазине.

---

### 3. Создать коммит (сохранить изменения)

```bash
git commit -m "Описание изменений"
```

**Хорошие примеры commit messages:**
```bash
git commit -m "feat: Add user authentication"
git commit -m "fix: Correct payment validation logic"
git commit -m "docs: Update installation guide"
git commit -m "chore: Update dependencies"
```

**Плохие примеры:**
```bash
git commit -m "update"           # ❌ Непонятно что изменили
git commit -m "fixes"            # ❌ Что именно починили?
git commit -m "asdf"             # ❌ Вообще непонятно
```

**Аналогия:** Сфотографировать изменения с описанием "что сделано".

---

### 4. Отправить на GitHub (Push)

```bash
git push origin название-ветки
```

**Пример для нашего проекта:**
```bash
git push origin chore/cleanup-all-green
```

**Аналогия:** Отправить посылку на почту (в облако GitHub).

---

### 5. Посмотреть историю коммитов

```bash
# Краткая история (одна строка на коммит)
git log --oneline

# Последние 5 коммитов
git log --oneline -5

# Подробная история
git log
```

**Пример вывода:**
```
9f77f46 chore: Update Claude Code settings with git permissions
b7e3516 feat: Add Big Four audit system and cleanup legacy modules
83aff5a Add Rust microservices implementation
```

---

## 🔄 Типичный рабочий процесс

### Сценарий: Вы изменили код и хотите сохранить изменения

```bash
# Шаг 1: Проверить что изменилось
git status

# Шаг 2: Посмотреть детали изменений
git diff

# Шаг 3: Добавить файлы
git add -A

# Шаг 4: Создать коммит
git commit -m "feat: Add new payment validation"

# Шаг 5: Отправить на GitHub
git push origin chore/cleanup-all-green
```

---

## 📝 Правила именования коммитов

Используйте префиксы для ясности:

| Префикс | Описание | Пример |
|---------|----------|--------|
| `feat:` | Новая функция | `feat: Add Excel export` |
| `fix:` | Исправление бага | `fix: Correct timezone handling` |
| `docs:` | Документация | `docs: Update API reference` |
| `chore:` | Рутинные задачи | `chore: Update dependencies` |
| `refactor:` | Рефакторинг кода | `refactor: Simplify auth logic` |
| `test:` | Добавление тестов | `test: Add unit tests for ledger` |
| `perf:` | Улучшение производительности | `perf: Optimize database queries` |

---

## 🌿 Работа с ветками

### Посмотреть текущую ветку
```bash
git branch --show-current
```

### Создать новую ветку
```bash
git checkout -b feature/new-feature
```

### Переключиться на другую ветку
```bash
git checkout main
```

### Список всех веток
```bash
git branch -a
```

---

## 🔍 Полезные команды для проверки

### Посмотреть изменения в конкретном файле
```bash
git diff путь/к/файлу.txt
```

### Посмотреть информацию о последнем коммите
```bash
git show
```

### Посмотреть кто и когда менял файл
```bash
git log --follow путь/к/файлу.txt
```

---

## 🆘 Частые проблемы и решения

### Проблема 1: "Я случайно изменил не тот файл"

**Решение:** Отменить изменения
```bash
git checkout -- путь/к/файлу.txt
```

**⚠️ Внимание:** Это удалит все несохранённые изменения!

---

### Проблема 2: "Я добавил файл в git add, но хочу убрать"

**Решение:** Убрать из staging area
```bash
git restore --staged путь/к/файлу.txt
```

---

### Проблема 3: "Я сделал коммит, но забыл добавить файл"

**Решение:** Изменить последний коммит
```bash
git add забытый-файл.txt
git commit --amend --no-edit
```

**⚠️ Внимание:** Используйте только если ещё не сделали push!

---

### Проблема 4: "Нужно вернуться к предыдущей версии"

**Решение 1:** Посмотреть старую версию файла
```bash
git show commit-id:путь/к/файлу.txt
```

**Решение 2:** Создать новый коммит, отменяющий изменения
```bash
git revert commit-id
```

---

## 📊 Статус нашего проекта

### Текущая ветка
```
chore/cleanup-all-green
```

### Последние коммиты
```
9f77f46 - chore: Update Claude Code settings with git permissions (сегодня)
b7e3516 - feat: Add Big Four audit system and cleanup legacy modules (сегодня)
83aff5a - Add Rust microservices implementation (2 недели назад)
```

### GitHub репозиторий
```
https://github.com/ROks-fin/Deltrran-MVP
```

---

## 🎓 Объяснение простыми словами

### Git - это как "Машина времени" для кода

- **Коммит** = Сохранённая точка, к которой можно вернуться
- **Ветка** = Параллельная версия проекта для экспериментов
- **Push** = Отправить изменения на сервер (GitHub)
- **Pull** = Скачать изменения с сервера
- **Merge** = Объединить две ветки

### Зачем нужен Git?

1. **История изменений** - можно посмотреть, что было раньше
2. **Резервная копия** - код хранится на GitHub
3. **Совместная работа** - несколько человек могут работать вместе
4. **Эксперименты** - можно попробовать что-то новое в отдельной ветке
5. **Откат изменений** - если что-то сломалось, можно вернуться назад

---

## 🔗 Полезные ссылки

- **GitHub репозиторий:** https://github.com/ROks-fin/Deltrran-MVP
- **Текущая ветка:** https://github.com/ROks-fin/Deltrran-MVP/tree/chore/cleanup-all-green
- **GitHub Desktop (GUI):** https://desktop.github.com/
- **Git Documentation:** https://git-scm.com/doc

---

## ✅ Чек-лист перед коммитом

Перед каждым коммитом проверьте:

- [ ] Код компилируется без ошибок
- [ ] Тесты проходят (если есть)
- [ ] Удалены временные файлы и отладочные принты
- [ ] Commit message понятно описывает изменения
- [ ] Не добавлены пароли или секретные ключи

---

## 🚀 Автоматизация с Claude Code

Claude Code может помочь с Git:

1. Проверить систему перед коммитом
2. Создать подробное commit message
3. Отправить изменения на GitHub
4. Проверить историю коммитов

Просто скажите: "Создай новый коммит" и Claude выполнит всю процедуру!

---

**Последнее обновление:** 2025-10-14

**Автор:** DelTran Team with Claude Code
